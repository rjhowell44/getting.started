script:  
  - curl -s https://testspace-client.s3.amazonaws.com/testspace-linux.tgz | sudo tar -zxvf- -C /usr/local/bin
  - testspace analysis.xml [tests]*test.xml coverage.xml $TESTSPACE_TOKEN/my-space#travis.Build.${TRAVIS_BUILD_NUMBER}